EXPO_PUBLIC_API_URL=http://192.168.1.x:8000/api
# Replace 192.168.1.x with your machine's LAN IP address.
# Run: ipconfig (Windows) | ifconfig (Mac/Linux) to find it.
# Physical devices must be on the same WiFi network.
# Emulators can use 10.0.2.2:8000 (Android) or localhost:8000 (iOS simulator).
